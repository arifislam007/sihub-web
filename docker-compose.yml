services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sombhabona_hub_web
    ports:
      - "8080:80"
    volumes:
      - ./:/var/www/html
    environment:
      APP_ENV: development
    restart: unless-stopped
